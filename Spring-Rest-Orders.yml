server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Order_db
    username: root
    password: tiger
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
